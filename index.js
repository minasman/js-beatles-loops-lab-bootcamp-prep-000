function theBeatlesPlay(musicians, instruments){
  var arr = [];
  for(var i=0; i<musicians.length; i++){
    var newString = `${musicians[i]} plays ${instruments}`
    arr.push(newString);
  }
  return arr;
}

function johnLennonFacts(facts){
  while (var i < facts.length) do{
    
  }
}